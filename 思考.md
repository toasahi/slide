その北極星なら、「システムの情報を網羅する台帳」ではなく、「エージェントがインシデント調査を開始・継続するために必要なコンテキスト台帳」として設計した方がいいです。ここを間違えると、100システム分の巨大な資産管理DBを作ること自体が目的になりやすいです。

So thatで置くと、こうなります。

システム台帳に情報を記録する
So that: エージェントが「このシステムは何者で、どこで動き、何に依存し、どこを調べればよいか」を人に聞かず判断できる
So that: インシデント発生時に、調査開始までのコンテキスト収集時間を減らせる
So that: オンコールがシステム固有知識を持っていなくても調査できる

この目的から逆算すると、最初の1システムでは次の8カテゴリを洗い出すのがよいと思います。

1. Identity — 何のシステムか
    システムID、名称、役割、提供機能、ユーザー、重要度。エージェントが「何が壊れた可能性があるのか」を理解するためです。
2. Ownership — 誰が知っているか
    オーナーチーム、担当者、連絡先、エスカレーション先。調査不能になったときに「誰へ聞くか」を判断するためです。
3. Architecture — どういう構造か
    コンポーネント、AWSアカウント、リージョン、Kubernetes Cluster/Namespace、DB、Queue、外部サービスなど。重要なのは詳細な設計書そのものではなく、調査対象を絞れる粒度です。
4. Dependencies — 何に依存しているか
    upstream / downstream、API、DB、認証基盤、ネットワークなど。「この障害は自システムなのか依存先なのか」を切り分けるためです。
5. Observability — どこを見れば状態が分かるか
    Metrics、Logs、Traces、Sentry、Zabbix、Prometheus、Dashboard、代表的なアラート。単なるURLではなく「何を見る場所か」まで持たせたいです。
6. Deployment — 何が変更されるか
    Repository、CI/CD、デプロイ先、リリース方法、直近リリースの取得方法。インシデントでは「最近の変更」がかなり重要なので、これは優先度が高いです。
7. Operations — どう調査・復旧するか
    Runbook、確認コマンド、再起動、ロールバック、Feature Flag、既知障害。エージェントが「次に何をするか」を決めるコンテキストです。
8. Knowledge — 詳細情報はどこにあるか
    ADR、設計書、過去インシデント、GitHub Issue、Jira、READMEなど。台帳自体に全情報をコピーするのではなく、情報探索の起点にします。

ただし、ここでもう一段疑った方がいいポイントがあります。

「台帳に何を書くか」より先に、「エージェントがインシデント時にどんな問いに答えられなければならないか」を定義した方がいいです。

例えば、

「このアラートはどのシステムか？」
「ユーザー影響は何か？」
「どこで動いているか？」
「直近で何がリリースされたか？」
「どのログを見るべきか？」
「依存サービスに異常はないか？」
「過去に同じ障害はあったか？」
「復旧手順はあるか？」

というAgent Questionsを先に作ります。

そして、

Agent Question → 必要なContext → 台帳項目

という順番で設計します。

たとえば、

「直近で何が変更された？」
→ Repository / Deployment Pipeline / Deployment Target が必要
→ repository, deployment.pipeline, deployment.target を台帳に持つ

という形です。

これなら「この項目、本当に必要？」に対して、常に So that何？ を返せます。

さらに、最初の1システムで私なら項目を決めることをゴールにはしません。仮の台帳をエージェントへ渡して実際の過去インシデントを1件調査させ、

不足コンテキストをAgent自身に洗い出させる

ところまでやります。

そうすると、

過去インシデント → Agent調査 → 情報不足 → 台帳項目追加

というループになります。これは100システムへ展開するときにもかなり重要で、机上で完全なSchemaを設計するより、「調査に必要だった情報」という実績からSchemaを育てられるからです。

最初の論点は二択にすると分かりやすいです。

A. システムについて人間が知っておくべき情報を台帳化する
B. エージェントがインシデント調査するために必要な情報を台帳化する

北極星から見ると、私はかなり強く B を推します。

次にやるなら、実際の1システムを題材にして、「インシデント調査Agentが答えるべき質問 → 必要Context → システム台帳Schema」まで一緒に具体化するのが一番よいです。



その整理はかなり重要です。ただ、「静的 / 動的」の2分類だけだと、台帳に何を保持するかが少し曖昧になります。 Bを採るなら、私はもう一段分けます。

ポイントは、動的データそのものを台帳に持つのか、動的データへ到達するための情報を持つのかです。

たとえば、

* GitHub Repository → 台帳に保持する
* システム管理者 → 台帳に保持する
* CPU使用率 → 台帳には保持しない
* Prometheusのメトリクス → 台帳には保持しない
* ログ本文 → 台帳には保持しない

代わりに台帳には、

* メトリクスはどこにあるか
* 何のメトリクスを見るべきか
* どのラベルで対象システムを特定するか
* ログはどこにあるか
* どのindex / namespace / service名で検索するか

を持たせます。

つまり、私は次の3層で考えます。

1. Static Context

エージェントが「このシステムは何者か」を理解する情報です。

例:

* system_id
* system_name
* description
* owner
* repository
* AWS Account
* Kubernetes Namespace
* architecture
* dependencies
* runbook
* SLO

これは基本的にシステム台帳自身がSource of Truthになります。

2. Discovery Context

ここが一番重要だと思っています。

動的情報をどう取得するかをエージェントに教える情報です。

例えば、

observability:
  metrics:
    provider: prometheus
    endpoint: xxx
    identifiers:
      namespace: payment
      service: payment-api
  logs:
    provider: opensearch
    index_pattern: payment-*
    identifiers:
      service_name: payment-api
  errors:
    provider: sentry
    project: payment-api

エージェントから見ると、

「Payment APIに障害が起きている」

↓

台帳を見る

↓

metrics → Prometheus
namespace → payment
logs → OpenSearch
service_name → payment-api

↓

MCP / API / Toolで実データを取得

となります。

私はこれをAgent Routing Informationくらいの概念として明示的に持たせた方がいいと思います。

3. Runtime Context

実際のインシデント発生時に取得する情報です。

* CPU
* Memory
* Error Rate
* Latency
* Pod状態
* Logs
* Traces
* Deployment history
* Git commit
* Alert
* Feature Flag
* 依存サービスの状態

これは台帳には保存せず、その時点で取得する。

構造としてはこうです。

                System Registry
                      │
             ┌────────┴────────┐
             │                 │
       Static Context    Discovery Context
             │                 │
             │          「どこを調べる？」
             │                 │
             └────────┬────────┘
                      ↓
                    Agent
                      │
             Runtime Information
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
   Prometheus      OpenSearch      GitHub
    Metrics           Logs        Releases

ここで重要なのが、「標準ログ」を動的情報として台帳項目にしてしまわないことです。

例えば、

logs:
  url: https://...

だけではAgentには弱いです。

Agentが本当に欲しいのは、

logs:
  provider: opensearch
  purpose: application_logs
  index_pattern: prod-payment-*
  filters:
    service: payment-api
    environment: production
  timestamp_field: "@timestamp"
  correlation_fields:
    - trace_id
    - request_id

のような、どう調査すればいいかという意味情報です。

これはB、

エージェントがインシデント調査するために必要な情報を台帳化する

という方針とかなり相性がいいです。

そして、ここで1つ前提を疑いたいです。

静的情報だから台帳に入れる、動的情報だから外に置く、ではありません。

判断軸は、

Agentが調査するとき、この情報を毎回Discoveryするコストが高いか？

です。

例えば「直近リリース」は動的ですが、

deployment:
  repository: xxx
  workflow: deploy-production.yml
  environment: production

さえ台帳にあれば、Agent自身がGitHubから最新リリースを取得できます。

したがって、

台帳 = Knowledge Database

ではなく、

台帳 = Agentが各情報源を探索するためのContext / Routing Map

と定義すると、かなり設計が整理されます。

最初の1システムでは、私は以下の形で洗い出します。

Agentが知りたいこと	種類	台帳に持つもの	実データ取得元
このシステムは何か	Static	description	台帳
誰が担当か	Static	owner	台帳
コードはどこか	Static	repository	GitHub
最近何が変わったか	Runtime	repo / workflow情報	GitHub
エラーが増えているか	Runtime	metric query context	Prometheus
何のエラーか	Runtime	log search context	OpenSearch
依存先はどこか	Static	dependencies	台帳
過去にも発生したか	Runtime	incident search context	GitHub/Jira

この表を作ると、Schemaを考える前に「Agentに何を渡す必要があるか」がかなり見えてきます。

次のステップとしては、1システムについて 「Agent Question → Static / Discovery / Runtime → 台帳項目」 の一覧を20〜30個くらいまで具体化すると、かなり良い初期Schemaになります。